import { PageLoader } from "@/components/molecules/Loaders";

export default function Loading() {
  return (
    <PageLoader
      description="Preparing the next page with the selected SupplyED theme."
      title="Loading page"
    />
  );
}
