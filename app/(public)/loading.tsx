import { PageLoader } from "@/components/molecules/Loaders";

export default function PublicLoading() {
  return (
    <PageLoader
      description="Preparing the public SupplyED page with the selected theme."
      title="Loading page"
    />
  );
}
